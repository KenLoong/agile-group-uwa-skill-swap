# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Local DBs from ad-hoc runs
tags_dev.db
*.db

# Environment
.env

# User-uploaded post images
# Keep the directory in the repository via .gitkeep, but do not commit runtime uploads.
static/uploads/posts/*
!static/uploads/posts/.gitkeep