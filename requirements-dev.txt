# =============================================================================
# UWA Skill-Swap — development & test dependencies
# =============================================================================
#
# Install with:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# These packages are not required in production (Docker/lecturer demo) unless
# you are running the Selenium grid or static analysis in CI.
#
# =============================================================================


# Always install production pins first (single source of truth for versions).
-r requirements.txt


# -----------------------------------------------------------------------------
# End-to-end / browser automation
# -----------------------------------------------------------------------------
# Selenium 4.x: pin below next major; driver management varies by platform.
selenium>=4.18.0,<5.0.0


# -----------------------------------------------------------------------------
# (Optional) formatting & lint — enable when we wire pre-commit
# -----------------------------------------------------------------------------
# ruff>=0.4.0,<1.0.0
# mypy>=1.8.0,<2.0.0
# pre-commit>=3.6.0,<4.0.0


# -----------------------------------------------------------------------------
# (Optional) unit test extras (if we add pytest later alongside unittest)
# -----------------------------------------------------------------------------
# pytest>=8.0.0,<9.0.0
# pytest-cov>=5.0.0,<6.0.0
# Faker>=25.0.0,<26.0.0


# =============================================================================
# End of development requirements
# =============================================================================
